{
  "app_binding_version": 1,
  "notes": "Справочник типов контракта UI↔backend. Соответствует windows/app.go (generated wailsjs). Поле/тип не из этого файла появляется в wailsjs — это дефект генерации, чинить генератор, не типы здесь.",
  "LifecycleState": "stopped|starting|running|stopping|reloading|error",
  "BlockedReason": "no_validated_channel|probe_failed|config_invalid|all_channels_failed",
  "ProbeStepStatus": "pending|running|pass|fail|skipped",
  "SecretKind": "vless-uuid|hy2-password|hy2-obfs-password|vps-ssh-key|cf-api-token|custom",
  "SecretBackend": "file|dpapi|keyring",
  "SecretMeta": {
    "id": "string (v4 UUID, generated by backend)",
    "kind": "SecretKind",
    "title": "string",
    "hint": "string",
    "backend": "SecretBackend",
    "storedValue": "value backing: 'file' | 'dpapi' | 'keyring' | null",
    "fingerprint": "string",
    "createdAt": "RFC3339",
    "updatedAt": "RFC3339",
    "lastVerifiedAt": "RFC3339|null",
    "verifyStatus": "unverified|ok|failed",
    "verifyError": "string",
    "reveals": "int (де-факто аудит)"
  },
  "SecretVerifyRequest": { "id": "string" },
  "SecretVerifyResult": { "status": "unverified|ok|failed", "checkedAt": "RFC3339", "error": "string" },
  "SecretRevealResult": { "value": "string|null (null если storedValue не file — reveal через backend не поддерживается)", "error": "string" },
  "SecretVerifySpec": {
    "kinds": ["vless-uuid", "hy2-password", "hy2-obfs-password", "vps-ssh-key", "cf-api-token", "custom"],
    "noNetworkVerification": "факт существования значения не доказывает его актуальность — см. хинт в SecretsView"
  },
  "ChannelDescriptor": {
    "id": "string",
    "transport": "string (vless-ws-tls|hysteria2|...)",
    "server": "string (лейбл сервера; домены/адреса не хардкодятся в UI)",
    "port": "int",
    "validation": "planned|configured|locally-tested|live-verified|degraded|blocked|retired",
    "enabled": "bool"
  },
  "ProbeStep": { "name": "string", "status": "ProbeStepStatus", "detail": "string" },
  "ProbeReportView": { "ok": "bool", "steps": "ProbeStep[]" },
  "AppState": {
    "state": "LifecycleState",
    "blockedReason": "BlockedReason|null",
    "activeChannelId": "string|null",
    "activeChannel": "ChannelDescriptor|null",
    "proxyMode": "socks|tun|null",
    "probe": "ProbeReportView|null",
    "probeRunning": "bool",
    "probeLastAt": "RFC3339|null",
    "error": "string"
  },
  "LogLine": { "t": "RFC3339", "source": "app|engine|probe", "level": "debug|info|warn|error", "line": "string" },
  "TestResult": {
    "id": "string (test id)",
    "ok": "bool|null (null = not run / unavailable)",
    "detail": "string",
    "checkedAt": "RFC3339|null",
    "steps": "ProbeStep[] (опционально)"
  },
  "UiLogLine": { "t": "RFC3339", "level": "debug|info|warn|error", "text": "string" }
}
