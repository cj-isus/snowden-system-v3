# Секреты и артефакты — никогда не в git (PLAN.md §7)
secrets/
logs/
*.exe
frontend/node_modules/
frontend/dist/*
!frontend/dist/.gitkeep
build/bin/

# Служебные артефакты ядра (V2-046: cache.db от эксперимента PlatformWriter)
*.db
