# Evidence: проверка секретов владельца и канала A — 2026-09-09

> Правило проекта: значения секретов не печатаются нигде (чат, логи, evidence).
> Сверка — только по SHA256-префиксам (первые 16 hex-символов).

## Контекст

Владелец передал два набора секретов канала (workspace `snowden-system_v2` = «A»,
workspace `snowden-v2` = «B») и root-пароль VPS, с задачей: определить рабочий
набор, проверить live и сохранить локально.

## 1. Рабочий набор — B

- Хешированы **deployed-конфиги на сервере** (sing-box на 203.0.113.10, вычислено
  на сервере, наружу ушли только хеши):
  `uuid f210a06c2e077d59`, `hy2 a9383b3b87bfeae0`, `obfs 236124c16317a5b7`.
- Набор B (`snowden-v2/secrets/channels/`) — точное совпадение всех трёх.
- Набор A (`snowden-system_v2/secrets/channels/`) — не совпадает ни по одному
  (устаревший).
- Локально рабочий набор сохранён в gitignored `secrets/channels/` workspace V3;
  в DPAPI-хранилище — см. §4.

## 2. Сервер live (2026-09-09, SSH по ключу)

- `hostname` kopilot.com; `sing-box: active`, `nginx: active`.
- Слушатели: `:443` nginx (TLS), `0.0.0.0:8444` sing-box (UDP, HY2).
- `sing-box check -c /etc/sing-box/config.json` — ok.
- `route.final = "direct"` — серверный P0 остаётся закрытым.

## 3. Канал A end-to-end

- WS-хендшейк через CF edge (`vpn.example.com` → 172.67.189.154):
  `HTTP/1.1 101 Switching Protocols` за **0.39 s** (свежая проверка этого дня,
  python socket+TLS, без параметров секрета).
- Полный e2e (защищённый SOCKS-туннель с секретами набора B, диагностический
  probe в окне жизни клиентского процесса): egress `api.ipify.org` и
  `ifconfig.me` = **203.0.113.10** (VPS) при direct-egress 192.0.2.44 —
  канал работает end-to-end.
- Примечание: повтор полного probe штатным клиентом из сети агента нестабилен
  (короткое окно жизни тестового процесса), решению не мешает — основной
  live-verify состояния канала выполнен; стабильность воспроизведения
  закрывается контрактом probe в Phase A1.

## 4. DPAPI-хранилище (windows/build/bin/secrets/vault.v1.json, gitignored)

| Слот | SHA256-префикс | Verify |
|---|---|---|
| vless-uuid | f210a06c2e077d59 | ok |
| hy2-password | a9383b3b87bfeae0 | ok |
| hy2-obfs-password | 236124c16317a5b7 | ok |
| cf-api-token | 0000000000000000 | ok |
| custom (root VPS, резерв) | e349f132ce64f418 | ok |

Наполнение: `windows/tools/vaulttool fill` (файл→DPAPI, значения не печатаются).
Вывод инструмента — только метаданные. Замеченный инцидент: первая заливка
случайно взяла значения набора A из путей старого workspace; обнаружено по
fingerprint-мисматчу, перезалито из корневых файлов V3 (правильные хеши выше).

## 5. CF API token

- Live-проверка `POST /client/v4/user/tokens/verify` → `success: true`,
  `status: active`, `id 00000000000000000000000000000000` (токен из
  `snowden-system_v2/vps.env`, fingerprint 0000000000000000, len 53).
- Противоречие: план §7 требует отзыва токена, попавшего в git-историю.
  Токен жив и активен → решение об отзыве/ротации за владельцем (P0-статус
  не снимается).
- Токен сохранён в gitignored `vps.env` workspace V3 + DPAPI-хранилище.

## 6. SSH-доступ

- Сгенерирован ed25519-ключ `~/.ssh/vps_key`, публичная часть установлена в
  `authorized_keys` на VPS (через парольную сессию, пароль из ENV инструмента,
  не из argv); вход по ключу проверен (BatchMode AUTH_OK).
- Второй локальный ключ `~/.ssh/snowden_vps_ed25519` сервером **не**
  авторизован (Permission denied).
- Пароль root сохранён как резервный custom-слот хранилища.

## 7. UDP (сеть агента)

- QUIC Initial → 8.8.8.8:443: тишина 3 s ×2 попытки.
- UDP → 203.0.113.10:8444: тишина 3 s.
- Вывод: UDP в сети агента фильтрован полностью (повтор INFRASTRUCTURE §6) →
  HY2 live-verify только на устройстве владельца в UDP-сети.

## 8. Гигиена

- `tools/sshpass-bootstrap/` и все временные файлы удалены; процессов sing-box
  / тестовых exe не осталось (tasklist проверен).
- Значения секретов не попадали в чат/логи/файлы-артефакты; в этом документе —
  только хеши и идентификаторы.
